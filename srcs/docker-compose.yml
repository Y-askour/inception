version: '3.7'

services:

  # nginx setup
  nginx:
    build: ./requirements/nginx/
    image: nginx
    networks:
      - inception
    ports:
      - 443:443
    volumes:
      - /home/yaskour/data/wp_files:/var/www/html
    env_file:
      - env.env
    depends_on:
      - wordpress
    restart : always

  # mariadb setup
  mariadb:
    build: ./requirements/mariadb/
    image: mariadb
    networks:
      - inception
    ports:
      - 3306:3306
    env_file:
      - env.env
    volumes:
      - /home/yaskour/data/wp_database:/var/lib/mysql/word_db
    restart : always

  #wordpress setup
  wordpress:
    build: ./requirements/wordpress/
    image: wordpress
    networks:
      - inception
    ports:
      - 9000:9000
    env_file:
      - env.env
    depends_on:
      - mariadb
    volumes:
      - /home/yaskour/data/wp_files:/var/www/html
    restart: always



# network that holds all my containers
networks:
  inception:
    name : inception
    driver : bridge
